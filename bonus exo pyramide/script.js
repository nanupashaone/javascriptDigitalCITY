const triangle1 = parseInt(prompt('entrez le nombre de coté dans votre triangle');
const triangle2 = parseInt(prompt('entrez le nombre de coté dans votre triangle');
const triangle3 = parseInt(prompt('entrez le nombre de coté dans votre triangle');

if (triangle1 <= 0  || triangle2 <= 0  || triangle3 <=  0) {
    console.log('erreur triangle non valide');
} else { 
    if (triangle1 === triangle2 && triangle2 === triangle3)
}
